<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-title" content="Remind Bot">
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
    
    <title>Element Reminder Ultra</title>
    <style>
        :root {
            --bg-color: #f0f2f5;
            --card-bg: #ffffff;
            --text-color: #333333;
            --input-bg: #f8f9fa;
            --input-border: #e1e4e8;
            --primary: #007bff;
            --accent: #ff4757; 
            --history-bg: #eeeeee;
        }

        @media (prefers-color-scheme: dark) {
            :root {
                --bg-color: #121212;
                --card-bg: #1e1e1e;
                --text-color: #e0e0e0;
                --input-bg: #2d2d2d;
                --input-border: #444444;
                --primary: #4a90e2;
                --history-bg: #333333;
            }
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
            background-color: var(--bg-color);
            color: var(--text-color);
            display: flex;
            flex-direction: column;
            align-items: center;
            min-height: 100vh;
            margin: 0;
            padding: 15px;
            box-sizing: border-box;
            overscroll-behavior-y: none; 
        }

        .card {
            background: var(--card-bg);
            padding: 20px;
            border-radius: 20px;
            box-shadow: 0 8px 30px rgba(0,0,0,0.12);
            width: 100%;
            max-width: 400px;
        }

        h2 {
            text-align: center;
            margin: 0 0 15px 0;
            font-size: 1.3rem;
            opacity: 0.9;
        }

        .form-group { margin-bottom: 15px; }

        label {
            display: block;
            margin-bottom: 6px;
            font-weight: 700;
            font-size: 0.8em;
            opacity: 0.6;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        input {
            width: 100%;
            padding: 12px;
            border: 2px solid transparent;
            border-radius: 12px;
            background-color: var(--input-bg);
            color: var(--text-color);
            box-sizing: border-box;
            font-size: 16px;
            outline: none;
            transition: all 0.2s;
        }
        
        input:focus {
            background-color: var(--card-bg);
            border-color: var(--primary);
        }

        /* 2x2 Grid for Times */
        .quick-actions {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 10px;
            margin-bottom: 20px;
        }

        .pill-btn {
            background-color: var(--input-bg);
            border: 1px solid var(--input-border);
            color: var(--text-color);
            padding: 12px;
            border-radius: 12px;
            font-size: 0.9em;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.2s;
        }
        
        .pill-btn:active { transform: scale(0.96); background-color: var(--input-border); }

        /* Voice Input Wrapper */
        .input-wrapper {
            position: relative;
            display: flex;
            align-items: center;
        }
        
        .mic-btn {
            position: absolute;
            right: 8px;
            background: none;
            border: none;
            font-size: 1.2rem;
            cursor: pointer;
            padding: 5px;
            opacity: 0.7;
        }
        
        .mic-btn.listening {
            opacity: 1;
            animation: pulse 1.5s infinite;
        }

        @keyframes pulse {
            0% { transform: scale(1); }
            50% { transform: scale(1.2); }
            100% { transform: scale(1); }
        }

        /* Output Area */
        textarea {
            width: 100%;
            height: 65px;
            padding: 12px;
            border: 2px solid var(--input-border);
            border-radius: 12px;
            background-color: var(--bg-color);
            color: var(--text-color);
            font-family: 'Courier New', monospace;
            font-weight: bold;
            resize: none;
            box-sizing: border-box;
            font-size: 14px;
            margin-top: 10px;
        }

        .btn-copy {
            background-color: var(--primary);
            color: #fff;
            width: 100%;
            padding: 15px;
            border: none;
            border-radius: 12px;
            font-size: 16px;
            font-weight: 700;
            cursor: pointer;
            margin-top: 15px;
            box-shadow: 0 4px 15px rgba(0, 123, 255, 0.3);
        }

        .open-link {
            text-align: center;
            margin-top: 15px;
            font-size: 0.9em;
        }
        .open-link a {
            color: var(--primary);
            text-decoration: none;
            font-weight: 600;
        }

        /* History */
        .history-section {
            width: 100%;
            max-width: 400px;
            margin-top: 25px;
        }
        
        .history-title {
            font-size: 0.8em;
            font-weight: bold;
            opacity: 0.5;
            margin-bottom: 10px;
            display: flex;
            justify-content: space-between;
        }
        
        .history-item {
            background: var(--card-bg);
            padding: 12px;
            border-radius: 10px;
            font-size: 0.85em;
            color: var(--text-color);
            margin-bottom: 8px;
            cursor: pointer;
            border: 1px solid var(--input-border);
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }
    </style>
</head>
<body>

<div class="card">
    <h2>Element Remind</h2>

    <div class="quick-actions">
        <button class="pill-btn" onclick="setSmartTime(10, 0)">10:00 AM</button>
        <button class="pill-btn" onclick="setSmartTime(11, 0)">11:00 AM</button>
        <button class="pill-btn" onclick="setSmartTime(15, 30)">03:30 PM</button>
        <button class="pill-btn" onclick="setSmartTime(16, 0)">04:00 PM</button>
    </div>

    <div class="form-group">
        <label>When</label>
        <div style="display: flex; gap: 10px;">
            <input type="date" id="reminderDate" onchange="updatePreview()">
            <input type="time" id="reminderTime" onchange="updatePreview()">
        </div>
    </div>

    <div class="form-group">
        <label>Message</label>
        <div class="input-wrapper">
            <input type="text" id="reminderContent" placeholder="Say or type message..." onkeyup="updatePreview()" autocomplete="off">
            <button class="mic-btn" id="micBtn" onclick="toggleVoice()">🎙️</button>
        </div>
    </div>
    
    <textarea id="outputBox" readonly></textarea>
    
    <button class="btn-copy" id="copyBtn" onclick="copyAndSave()">Copy Command</button>
    
    <div class="open-link">
        <a href="https://app.element.io" target="_blank">Open Element Web &rarr;</a>
    </div>
</div>

<div class="history-section" id="historySection" style="display:none;">
    <div class="history-title">
        <span>RECENT HISTORY</span>
        <span style="cursor:pointer; color:var(--primary);" onclick="clearHistory()">Clear</span>
    </div>
    <div class="history-list" id="historyList"></div>
</div>

<script>
    // 1. Initialize
    const now = new Date();
    updateInputs(now);
    renderHistory();

    // Check if Voice API is supported
    if (!('webkitSpeechRecognition' in window) && !('SpeechRecognition' in window)) {
        document.getElementById('micBtn').style.display = 'none';
    }

    function updateInputs(dateObj) {
        const dateStr = dateObj.toISOString().split('T')[0];
        const hours = String(dateObj.getHours()).padStart(2, '0');
        const minutes = String(dateObj.getMinutes()).padStart(2, '0');
        
        document.getElementById('reminderDate').value = dateStr;
        document.getElementById('reminderTime').value = `${hours}:${minutes}`;
        updatePreview();
    }

    // 2. SMART TIME LOGIC
    function setSmartTime(hour, minute) {
        const today = new Date();
        // Create a date object for Today at the requested time
        let targetDate = new Date();
        targetDate.setHours(hour, minute, 0, 0);

        // If the requested time is earlier than right now, assume TOMORROW
        if (targetDate < new Date()) {
            targetDate.setDate(targetDate.getDate() + 1);
            showToast("Set for Tomorrow (Time passed)");
        }

        updateInputs(targetDate);
        
        // Visual feedback
        const timeInput = document.getElementById('reminderTime');
        timeInput.style.backgroundColor = "var(--primary)";
        setTimeout(() => { timeInput.style.backgroundColor = ""; }, 300);
    }

    // 3. VOICE INPUT LOGIC
    function toggleVoice() {
        const SpeechRecognition = window.SpeechRecognition || window.webkitSpeechRecognition;
        const recognition = new SpeechRecognition();
        const micBtn = document.getElementById('micBtn');

        recognition.lang = 'en-US';
        recognition.start();
        micBtn.classList.add('listening');

        recognition.onresult = function(event) {
            const transcript = event.results[0][0].transcript;
            const contentBox = document.getElementById('reminderContent');
            // Append text if box is not empty
            if(contentBox.value) {
                contentBox.value += " " + transcript;
            } else {
                contentBox.value = transcript;
            }
            updatePreview();
            micBtn.classList.remove('listening');
        };

        recognition.onerror = function(event) {
            micBtn.classList.remove('listening');
            alert("Voice input error. Try typing.");
        };
        
        recognition.onend = function() {
            micBtn.classList.remove('listening');
        };
    }

    function updatePreview() {
        const content = document.getElementById('reminderContent').value.trim();
        const date = document.getElementById('reminderDate').value;
        const time = document.getElementById('reminderTime').value;
        const safeContent = content ? content : "[message]";
        
        document.getElementById('outputBox').value = `!remind ${date} ${time} ${safeContent}`;
    }

    function copyAndSave() {
        const outputBox = document.getElementById("outputBox");
        const content = document.getElementById('reminderContent').value.trim();
        
        if(content) saveToHistory(content);

        outputBox.select();
        outputBox.setSelectionRange(0, 99999);
        navigator.clipboard.writeText(outputBox.value).then(() => {
            const btn = document.getElementById("copyBtn");
            const originalText = btn.innerText;
            btn.innerText = "✅ Copied!";
            btn.style.backgroundColor = "#28a745";
            setTimeout(() => {
                btn.innerText = originalText;
                btn.style.backgroundColor = "";
            }, 1500);
        });
    }

    function showToast(msg) {
        // Simple toast notification for Smart Time
        const btn = document.querySelector('.card h2');
        const oldText = btn.innerText;
        btn.innerText = "📅 " + msg;
        btn.style.color = "var(--primary)";
        setTimeout(() => {
            btn.innerText = oldText;
            btn.style.color = "";
        }, 2000);
    }

    // History Functions
    function saveToHistory(msg) {
        let history = JSON.parse(localStorage.getItem('remindHistory') || '[]');
        history = history.filter(item => item !== msg);
        history.unshift(msg);
        if(history.length > 5) history.pop();
        localStorage.setItem('remindHistory', JSON.stringify(history));
        renderHistory();
    }

    function renderHistory() {
        const history = JSON.parse(localStorage.getItem('remindHistory') || '[]');
        const container = document.getElementById('historyList');
        const section = document.getElementById('historySection');
        
        if (history.length === 0) {
            section.style.display = 'none';
            return;
        }
        
        section.style.display = 'block';
        container.innerHTML = '';
        
        history.forEach(msg => {
            const div = document.createElement('div');
            div.className = 'history-item';
            div.innerText = msg;
            div.onclick = () => {
                document.getElementById('reminderContent').value = msg;
                updatePreview();
                window.scrollTo({ top: 0, behavior: 'smooth' });
            };
            container.appendChild(div);
        });
    }

    function clearHistory() {
        localStorage.removeItem('remindHistory');
        renderHistory();
    }
</script>

</body>
</html>
